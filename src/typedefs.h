#ifndef TYPEDEFS_H
#define TYPEDEFS_H

typedef struct
{
    float x;
    float y;
} vec2f;

typedef struct
{
    unsigned char red;
    unsigned char green;
    unsigned char blue;
} color;

#endif  // #ifndef TYPEDEFS_H